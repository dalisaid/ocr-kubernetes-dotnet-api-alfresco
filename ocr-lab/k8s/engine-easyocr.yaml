apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-easyocr
  namespace: ocr-lab
  labels:
    app: engine-easyocr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-easyocr
  template:
    metadata:
      labels:
        app: engine-easyocr
    spec:
      containers:
        - name: engine-easyocr
          image: ocr-easyocr:latest
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8002

          env:
            # ── Worker infrastructure ──────────────────────────
            - name: RABBITMQ_URL
              value: "amqp://admin:admin@rabbitmq:5672/"
            - name: REDIS_URL
              value: "redis://redis-master:6379"
            - name: JOB_TTL
              value: "3600"
            # .NET API is outside the cluster on the host machine
            - name: DOTNET_API_URL
              value: "http://host.docker.internal:5122/ocrdata"
            # ── Observability ──────────────────────────────────
            - name: OTEL_ENDPOINT
              value: "otel-collector-opentelemetry-collector:4317"

          startupProbe:
            httpGet:
              path: /healthz
              port: 8002
            failureThreshold: 30
            periodSeconds: 20

          readinessProbe:
            httpGet:
              path: /healthz
              port: 8002
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /healthz
              port: 8002
            initialDelaySeconds: 30
            periodSeconds: 20

          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2000m"
              memory: "3Gi"

---
apiVersion: v1
kind: Service
metadata:
  name: engine-easyocr
  namespace: ocr-lab
spec:
  selector:
    app: engine-easyocr
  ports:
    - port: 8002
      targetPort: 8002
